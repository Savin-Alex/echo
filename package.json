{
  "name": "echo-copilot-root",
  "version": "1.0.0",
  "private": true,
  "description": "Echo - AI Interview, Meeting & Workplace Copilot",
  "author": "CriticalSuccess",
  "license": "ISC",
  "workspaces": [
    "electron-app"
  ],
  "scripts": {
    "dev": "concurrently -n PY,ELECTRON \"npm run dev:python\" \"npm run dev:electron\"",
    "dev:python": "cd python-workers && .venv/bin/uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:electron": "cd electron-app && npm run dev",
    "build": "npm run build:python && npm run build:electron",
    "build:python": "cd python-workers && pip install -r requirements.txt",
    "build:electron": "cd electron-app && npm run build",
    "test": "npm run test:python && npm run test:electron",
    "test:python": "cd python-workers && pytest",
    "test:electron": "cd electron-app && npm test",
    "lint": "npm run lint:python && npm run lint:electron",
    "lint:python": "cd python-workers && ruff check . || echo 'Ruff not installed, skipping Python lint'",
    "lint:electron": "cd electron-app && npm run lint",
    "setup:electron": "cd electron-app && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "keywords": [
    "ai",
    "copilot",
    "interview",
    "meeting",
    "assistant",
    "whisper",
    "ollama",
    "electron",
    "react"
  ]
}
