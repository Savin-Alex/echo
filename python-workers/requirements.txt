# Core Requirements (minimal install to get app running)
# These install quickly and don't require system dependencies

# FastAPI Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.26.0
python-multipart>=0.0.6

# Basic utilities
python-dotenv>=1.0.0
aiofiles>=23.2.0
Pillow>=10.2.0
numpy>=1.26.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

# ============================================================================
# Optional AI Dependencies (install separately after core setup)
# See requirements-ai.txt for full AI capabilities
# ============================================================================
#
# To install AI features after core setup:
#   source .venv/bin/activate  # macOS/Linux
#   pip install -r requirements-ai.txt
#
# Or install individually:
#   pip install faster-whisper        # For ASR (requires ffmpeg)
#   pip install sentence-transformers # For embeddings
#   pip install chromadb              # For vector store
#   pip install pytesseract           # For OCR (requires tesseract)
#   pip install librosa               # For voice analysis
